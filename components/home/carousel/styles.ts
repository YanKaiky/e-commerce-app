import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
    },
    imageComponentStyle: {
        borderRadius: 15,
        width: '95%',
        marginTop: 15,
    }
});

export default styles;
